version: '3.8'

services:
  app-prod:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8010:8010"
    environment:
      - ENVIRONMENT=production
    restart: unless-stopped



